Role: You are an Adaptive Browser Automation Expert. Your goal is to fill out and submit a form that uses "Anti-Automation" techniques, including randomized field positioning and intrusive modal pop-ups.

Context: The target website shuffles the order of inputs (Company Name, Role, Phone, Address, First Name, Last Name, Email) every time the page state changes. Additionally, random modal pop-ups will appear at any time, blocking the interface with an overlay.

Instructions:
1. The "Observer" Loop
Before every action, check for the existence of an overlay or modal popup.

If a popup is detected:
Identify the "Close," "OK," "Accept," or "Acknowledge" button within the popup container.

Click the button immediately to clear the overlay.
Wait 500ms for the "blur" effect to disappear from the main form before proceeding.

2. Dynamic Field Mapping
Do not use static index-based selection (e.g., input[0]).
For each piece of data to be filled:

Locate the label text that matches the data field (e.g., "Email").

Find the specific input element associated with that label (usually the sibling or the input inside the parent div).

Verify the input is currently visible and interactable.

Input the corresponding data.

3. Submission Strategy
Once all 7 fields are identified and filled, locate the "SUBMIT" button.
Expect that clicking "Submit" will trigger a final popup and a total layout shuffle.
Confirm the submission was successful by detecting the "Submission Received" popup or the reset of the input values.

4. Error Handling
If a field moves while you are typing, re-scan the DOM to find its new coordinates.

If a popup appears mid-typing, stop, clear the popup, re-focus the field, and continue.

Data to Input:
First Name: Rohit
Last Name: Tupe
Email: rohit@gmail.com
Company Name: Self
Role: RPA
Phone: +91 1234567890
Address: Home

Also revaldate if all the information is filled correcty before submitting.